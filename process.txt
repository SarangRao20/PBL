process carried out : 
- phase 1: 
	making of the virtual environment
	making of the requirements.txt and clean installing them 

	getting an api key from openrouter for later use

-phase 2:
	Data Ingestion ( pdf first, paper aligned)

	we need to convert module content -> clean text chunks

	sources could be pdfs ( handbooksm syllabus, notes etc),
	webpages,
	announcements

	in phase 2,  we focus on the pdf ingestion pipeline 

	we firstly import the pypdf library

	then we made the chunking of the pdf text and then made the embedding of the text and stored it in the 
	embedding folder as a csv

	What Phase 4 does (conceptually)

Load stored embeddings (CSV)

Convert embeddings from text → vectors

Embed the user query (same model!)

Compute cosine similarity

Rank chunks

Produce a relatedness score (%)

This is exactly what the paper describes as spatial distance–based ranking.




	